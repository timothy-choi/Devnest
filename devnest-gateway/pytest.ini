[pytest]
testpaths = tests
# Tests use ``import route_admin_app``; source is ``route_admin/route_admin_app.py``.
pythonpath = route_admin
# Match backend: bounded tests, continue after failures, pytest-timeout default.
timeout = 300
timeout_method = thread
addopts = -ra --maxfail=0 --timeout-method=thread
