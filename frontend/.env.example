NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# Local `next dev`: do NOT set INTERNAL_API_BASE_URL=http://backend:8000 (hostname only works inside Compose).
# Docker Compose sets DOCKER_CONTAINER=true and uses http://backend:8000 for API routes automatically.
# INTERNAL_API_BASE_URL=
